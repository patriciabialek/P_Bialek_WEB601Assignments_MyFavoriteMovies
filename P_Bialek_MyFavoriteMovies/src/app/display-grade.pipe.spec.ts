import { DisplayGradePipe } from './display-grade.pipe';

describe('DisplayGradePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayGradePipe();
    expect(pipe).toBeTruthy();
  });
});
